# Archive

Throwaway and superseded artifacts kept for reference, not product SSOT.

| Path | Purpose |
|---|---|
| `phase-0/driver-tree-studio.tsx` | Phase 0 Claude Artifacts demo; logic extracted to `driver_tree_studio/` |

Run the demo: `cd demo && npm install && npm run dev`.
