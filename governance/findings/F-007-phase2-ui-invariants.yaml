id: F-007
title: "Phase 2 UI: clipped node labels, leaf formula sync, reconciliation badge"
category: partial_migration
kind: recurrence_invariant
status: closed
opened_at: 2026-06-08
closed_at: 2026-06-08
recurrence_count: 0
evidence: |
  driver_tree_studio/src/components/Canvas/MetricNode.tsx — justify-between in 56px
    node collapsed metric name text (top half only visible on all nodes).
  driver_tree_studio/app/page.tsx — setFormulaParentId(leaf) on node click produced
    empty identity formulas (e.g. average_order_value =) and parseFormula errors
    on numeric literals (unexpected char '1' at position 0).
  driver_tree_studio/src/hooks/useMetricModel.ts — reconciled gated on validation.valid,
    surfacing [controllable-leaf] schema warnings under "Identity reconciled" badge.
verification_script: tests/governance/test_f007_phase2_ui_invariants.py
notes: |
  Remediation: MetricNode flex layout with flex-1 name row; FormulaField uses
  defaultFormulaParent for leaf selection and clears sync errors; AppHeader splits
  identityReconciled from validationWarnings. Redeploy driver_tree_studio to Vercel
  after merge — production still serves pre-fix build until then.
